# assemblies/

Assemblies-as-code via **partcad** — compose `parts/` into positioned assemblies
with a versioned dependency graph. This graph is what the agent mutates when it
merges parts or swaps variants (see CONCEPT.md §6). Populated in PLAN.md Phase 0+.
